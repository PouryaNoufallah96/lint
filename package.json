{
  "name": "lint-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @shadcn/lint run build",
    "test": "pnpm --filter @shadcn/lint run build && pnpm --filter @shadcn/lint run test",
    "typecheck": "pnpm --filter @shadcn/lint exec tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format:write": "prettier --write . --cache",
    "format:check": "prettier --check . --cache",
    "corpus": "pnpm --filter @shadcn/lint run build && pnpm --filter evals run corpus",
    "corpus:check": "pnpm --filter @shadcn/lint run build && pnpm --filter evals run corpus:check",
    "evals": "pnpm --filter @shadcn/lint run build && pnpm --filter evals run run",
    "changeset": "changeset",
    "release": "pnpm run typecheck && pnpm run test && pnpm run corpus:check && changeset publish"
  },
  "engines": {
    "node": ">=20.19"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^3.0.1",
    "@eslint/js": "^9.33.0",
    "@ianvs/prettier-plugin-sort-imports": "^4.7.1",
    "eslint": "^9.33.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^16.3.0",
    "prettier": "^3.6.2",
    "typescript-eslint": "^8.40.0"
  }
}